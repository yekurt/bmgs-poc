#eureka:
#    client:
#        serviceUrl:
#            defaultZone: http://localhost:8761/eureka/
spring:
    cloud:
        config:
            discovery:
                enabled: true
                service-id: config
            name: telemetry-service
        consul:
            host: localhost
            port: 8500