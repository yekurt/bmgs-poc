#eureka:
#    client:
#        serviceUrl:
#            defaultZone: http://localhost:8761/eureka/
spring:
    cloud:
        config:
            discovery:
                enabled: true
                service-id: config
            name: accountmanagement-service
        consul:
            host: localhost
            port: 8500
